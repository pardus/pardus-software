polkit.addRule(function(action, subject) {
    if (subject.isInGroup("pardus-software")) {
        if (action.id == "tr.org.pardus.pkexec.pardus-software-action") {
            return polkit.Result.YES;
        }
    }
});
